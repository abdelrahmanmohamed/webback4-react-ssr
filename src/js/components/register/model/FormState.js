export default class FormState {
    constructor(currentStep, email, username, password) {
        this.currentStep = currentStep;
        this.email = email;
        this.username = username;
        this.password = password;
    }
}
